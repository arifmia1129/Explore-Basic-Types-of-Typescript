const family: string[] = ['Arif', 'Binu', 'Ariba', 'Abdullah'];

const familyMembersAge: number[] = [25, 23, 2, 1];

// only use those method that only related with this type

// family.map(member => member.)
// familyMembersAge.map(age => age.)

// touple
const student: [string, number, string] = ['Ariba', 1, 'female'];

student[0] = 'Ariba Binte Arif';
